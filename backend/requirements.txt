fastapi
uvicorn[standard]
pydantic[email]>=2
pydantic-settings

# Phase 2: database
sqlalchemy>=2
alembic
psycopg2-binary

# Phase 3: auth (Firebase — superseded; removed in Phase 4 step 4)
firebase-admin

# Phase 4: own-JWT auth, providers, structured logging
pyjwt
passlib[bcrypt]
structlog
httpx
cryptography
