# Dev/test-only deps. Install with:
#     pip install -r requirements-dev.txt
#
# Keep app deps in requirements.txt — those land in the prd Docker image too.
-r requirements.txt

pytest>=8
pytest-asyncio>=0.23
pytest-cov>=4

# §5.3 dev AI usage log — image width/height. Optional: the logger degrades to
# size+mime only if it's missing.
Pillow>=10
