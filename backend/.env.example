ENVIRONMENT=local

# Phase 2: matches docker-compose.yml (host port 5433 to avoid conflicts)
DATABASE_URL=postgresql+psycopg2://admin:password@localhost:5433/drape

# Phase 3: required when ENVIRONMENT != local
# FIREBASE_CREDENTIALS_PATH=/absolute/path/to/serviceAccount.json

# Phase 4+
# OPENAI_API_KEY=sk-...
