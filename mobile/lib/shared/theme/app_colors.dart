import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ─── Brand ──────────────────────────────────────────────────────
  static const Color espresso = Color(0xFF6B4530);
  static const Color espressoDark = Color(0xFF512F1B);
  static const Color espressoDeep = Color(0xFF18100A);
  static const Color brandText = Color(0xFFF5EDE4);

  // ─── Neutrals (warm ivory family) ───────────────────────────────
  static const Color ivory = Color(0xFFFDF9F4);
  static const Color ivoryDim = Color(0xFFF7F3EE);
  static const Color ivoryWarm = Color(0xFFF1EDE8);
  static const Color sand = Color(0xFFE6E2DD);
  static const Color tan = Color(0xFFE9B499);
  static const Color tanFixed = Color(0xFFFFDBCA);

  // ─── Text ───────────────────────────────────────────────────────
  static const Color ink = Color(0xFF1C1C19);
  static const Color inkSoft = Color(0xFF51443E);
  static const Color taupe = Color(0xFF83746D);
  static const Color taupeSoft = Color(0xFFD5C3BB);
  static const Color taglineGrey = Color(0xFF9C8878);

  // ─── Accent: sage (secondary) ───────────────────────────────────
  static const Color sage = Color(0xFF53643A);
  static const Color sageDim = Color(0xFFD5EAB4);
  static const Color sageContent = Color(0xFF596A3F);

  // ─── Accent: gold (tertiary) ────────────────────────────────────
  static const Color goldDark = Color(0xFF4C3300);
  static const Color gold = Color(0xFFE4BA77);
  static const Color goldSoft = Color(0xFFEBC07D);

  // ─── Status ─────────────────────────────────────────────────────
  static const Color error = Color(0xFFBA1A1A);
  static const Color errorContainer = Color(0xFFFFDAD6);
  static const Color onErrorContainer = Color(0xFF93000A);

  // ─── Absolute ───────────────────────────────────────────────────
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
}
